<Page x:Class="Vending.AdminFunc.Drinks"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Vending.AdminFunc"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Drinks">

    <Grid>
        <ListView ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="LViewDrinks_SelectionChanged"  Name="LViewDrinks">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"></WrapPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="10" Orientation="Vertical">
                        <Image Width="150" Source="{Binding Image}" Height="150" Stretch="Uniform" Margin="5" HorizontalAlignment="Center">
                            
                        </Image>
                        <TextBlock Text="{Binding Name}" />
                        <TextBlock Text="{Binding Cost}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
